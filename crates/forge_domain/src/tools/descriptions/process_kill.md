Stops a managed background process. The `process_id` handle comes from a shell timeout handoff when a shell command exceeds the synchronous execution window.

Use this to clean up servers, watchers, or long-running commands after verification. The result reports the final process status.
